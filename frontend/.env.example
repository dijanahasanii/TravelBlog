# Copy this file to .env and fill in your values
# ── Backend service URLs ───────────────────────────────────────────────────────
# Local development (defaults — no change needed for local):
REACT_APP_USER_SERVICE_URL=http://localhost:5004
REACT_APP_CONTENT_SERVICE_URL=http://localhost:5002
REACT_APP_NOTIF_SERVICE_URL=http://localhost:5006

# Production example (replace with your deployed URLs):
# REACT_APP_USER_SERVICE_URL=https://user-service.yourapp.com
# REACT_APP_CONTENT_SERVICE_URL=https://content-service.yourapp.com
# REACT_APP_NOTIF_SERVICE_URL=https://notif-service.yourapp.com

# ── Cloudinary (optional) ──────────────────────────────────────────────────────
# Without these, images/videos are stored as base64 in MongoDB (works but slower)
# 1. Create free account at https://cloudinary.com
# 2. Settings → Upload → Upload Presets → Add Preset (Unsigned)
# 3. Fill in your Cloud Name and Preset Name below
REACT_APP_CLOUDINARY_CLOUD_NAME=your_cloud_name
REACT_APP_CLOUDINARY_UPLOAD_PRESET=your_unsigned_preset_name
