# Copy this file to .env and fill in your values

# ── JWT secrets (backend) ─────────────────────────────────────────────────────
JWT_SECRET=your_super_secret_jwt_key_here
JWT_REFRESH_SECRET=your_super_secret_refresh_key_here

# ── Cloudinary (frontend) — for image/video hosting via CDN ──────────────────
# 1. Create a free account at https://cloudinary.com
# 2. Go to Settings → Upload → Upload Presets → Add Upload Preset
# 3. Set "Signing Mode" to "Unsigned" and save
# 4. Copy the Cloud Name (from dashboard) and the Preset Name below
# 5. Create frontend/.env with these values (REACT_APP_ prefix required)
REACT_APP_CLOUDINARY_CLOUD_NAME=your_cloud_name
REACT_APP_CLOUDINARY_UPLOAD_PRESET=your_unsigned_preset_name

# ── VAPID keys (notification-service) ────────────────────────────────────────
# Pre-generated defaults are baked in. Regenerate for production:
#   node -e "const wp=require('web-push'); const k=wp.generateVAPIDKeys(); console.log(k)"
VAPID_PUBLIC_KEY=BH0RjhWx-wImvCTAXnCJ3VnXCB1DJ5h4IFwFIMHWPusB6DzM4BPiqDRAFgZKeexzjdij98IHzBCeowNXxjgHog8
VAPID_PRIVATE_KEY=eW36tbRtj_mVUm8nYDCelMtvipsxNpQtrOlh9s41ZE0
